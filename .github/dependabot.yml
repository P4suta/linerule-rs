version: 2

# Dependabot owns scheduled dependency updates. It bumps Cargo crates weekly
# and pins GitHub Actions to fresh SHAs.
#
# Every entry pins commit-message.prefix to "build(deps)" so the
# `conventional commits` CI gate accepts Dependabot-authored commits.

updates:
  - package-ecosystem: cargo
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    commit-message:
      prefix: "build(deps)"
    groups:
      tracing:
        patterns:
          - "tracing*"
      serde:
        patterns:
          - "serde*"
      test:
        patterns:
          - "proptest"
          - "tempfile"
      windows:
        patterns:
          - "windows"
          - "windows-core"
          - "windows-*"

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: "build(deps)"
