[alias]
xtask = "run --quiet --package xtask --"

[build]
# Default target stays native; CI overrides for cross checks.

[target.x86_64-pc-windows-msvc]
# cargo-xwin が自動補完するため空。必要に応じて linker を上書き。

[term]
color = "auto"
